---
name: sheep-expert
description: 羊专家（ComfyUI生图核心专家）技能包，负责提示词工程、ControlNet精准控制、LoRA模型应用。包含提示词工程、ControlNet精准控制、LoRA模型应用等功能。是十二生肖团的生图核心。
version: 1.0.0
author: 速凡团队（浙江永康）
last_updated: 2026-06-06
tags: [sheep-expert, prompt-engineering, controlnet, lora, comfyui, twelve-zodiac-team]
dependencies: [python>=3.8, json, argparse, comfyui]
---

# 羊专家（ComfyUI生图核心专家）技能包#

## 功能概述#

本技能包提供 ComfyUI 生图功能，包含：

1. **提示词工程** - 提示词编写、优化、分类管理
2. **ControlNet精准控制** - ControlNet配置、控制器选择、精准控制
3. **LoRA模型应用** - LoRA模型选择、应用、参数调优

羊专家是十二生肖团的**生图核心**，负责生成高质量产品效果图。

## 详细功能说明#

### 1. 提示词工程
编写、优化、分类管理提示词，用于ComfyUI生图。

**提示词分类**：
- **材质提示词**：描述产品材质（如：镁合金、碳纤维、不锈钢等）
- **功能提示词**：描述产品功能（如：弹跳盖、温度显示、防滑底座等）
- **风格提示词**：描述产品风格（如：简约风格、卡通风格、商务风格等）
- **场景提示词**：描述产品使用场景（如：车载场景、办公场景、户外场景等）

**提示词编写技巧**：
- 使用逗号分隔关键词
- 使用括号调整关键词权重（如：`(magnesium alloy:1.2)`）
- 使用负面提示词（如：`negative_prompt="low quality, blurry"`）

### 2. ControlNet精准控制
配置ControlNet，实现精准控制生成图。

**ControlNet类型**：
- **Canny**：边缘检测，精准控制产品轮廓
- **Depth**：深度图，精准控制产品立体感
- **Normal**：法线图，精准控制产品表面细节
- **Sketch**：草图，精准控制产品线条

**ControlNet配置**：
- 选择控制器（如：Canny、Depth等）
- 设置控制器权重（如：0.5-1.5）
- 设置控制器分辨率（如：512x512、768x768等）

### 3. LoRA模型应用
选择、应用、调优LoRA模型，提升生成图质量。

**LoRA模型类型**：
- **材质LoRA**：提升材质真实感（如：镁合金LoRA、碳纤维LoRA等）
- **功能LoRA**：提升功能准确性（如：弹跳盖LoRA、温度显示LoRA等）
- **风格LoRA**：提升风格一致性（如：简约风格LoRA、卡通风格LoRA等）

**LoRA模型应用**：
- 选择LoRA模型（根据产品类型）
- 设置LoRA权重（如：0.5-1.5）
- 调整LoRA参数（如：触发词、应用层等）

## 安装 (Installation)#

1. 确保已安装 Python 3.8+。
2. 安装 ComfyUI 及依赖：`pip install comfyui`
3. 下载 ControlNet 模型：`下载地址`
4. 下载 LoRA 模型：`下载地址`

## 使用方法 (Usage)#

### 命令行使用 (Command Line Usage)#

```bash
# 提示词工程（编写提示词）
python scripts/prompt_engineer.py --product "vacuum cup" --material "magnesium alloy" --function "pop-up lid" --style "minimalist" --output "data/prompts.json"

# ControlNet精准控制（配置ControlNet）
python scripts/controlnet_controller.py --image "data/reference_images/cup_001.jpg" --control_type "canny" --weight 1.0 --output "data/controlnet_config.json"

# LoRA模型应用（应用LoRA模型）
python scripts/lora_applier.py --product "vacuum cup" --lora_type "material" --lora_weight 1.0 --output "data/lora_config.json"

# 生成产品效果图（综合使用提示词、ControlNet、LoRA）
python scripts/image_generator.py --prompt "data/prompts.json" --controlnet "data/controlnet_config.json" --lora "data/lora_config.json" --output "data/generated_images/"
```

### 输出文件格式#

#### 1. 提示词文件（prompts.json）
```json
{
  "product": "vacuum cup",
  "prompts": {
    "positive": "vacuum cup, magnesium alloy, pop-up lid, minimalist style, high quality, photorealistic",
    "negative": "low quality, blurry, distorted, deformed"
  },
  "weights": {
    "magnesium alloy": 1.2,
    "pop-up lid": 1.1
  }
}
```

#### 2. ControlNet配置文件（controlnet_config.json）
```json
{
  "control_type": "canny",
  "model": "controlnet-canny-sd15",
  "weight": 1.0,
  "resolution": "512x512",
  "preprocessor": "canny"
}
```

#### 3. LoRA配置文件（lora_config.json）
```json
{
  "lora_type": "material",
  "model": "magnesium-alloy-lora-v1.safetensors",
  "weight": 1.0,
  "trigger_word": "magnesium alloy"
}
```

#### 4. 生成图文件
**目录结构**：
```
data/generated_images/
├─ cup_001.png           # 生成图文件
├─ cup_002.png
└─ metadata.json          # 元数据文件（生成参数、配置等）
```

**元数据文件格式**（metadata.json）：
```json
{
  "image_file": "cup_001.png",
  "prompts": {...},
  "controlnet": {...},
  "lora": {...},
  "generation_params": {
    "steps": 30,
    "cfg_scale": 7.5,
    "sampler": "DPM++ 2M Karras",
    "seed": 123456789
  }
}
```

## 与其他技能包的数据传输#

### 输入（接收其他技能包的数据）
- **马专家**：接收ComfyUI工作流文件（含节点配置）
- **猴专家**：接收参数调优结果（含优化后的参数配置）
- **鸡专家**：接收评审报告（含改进建议）

### 输出（向其他技能包传输数据）
- **猴专家**：输出生成图文件（供参数调优）
- **鸡专家**：输出生成图文件（供质量评审）
- **鼠专家**：输出最终生成图文件（供最终输出）

### 数据传输格式
- **文件传输**：生成图文件（PNG格式）、配置文件（JSON格式）
- **元数据传输**：JSON文件（metadata.json）

## 案例分析#

### 案例1：生成弹跳盖保温杯效果图
**需求**：生成弹跳盖保温杯的效果图

**执行步骤**：
1. 马专家输出ComfyUI工作流文件：`data/workflows/cup_workflow.json`
2. 羊专家执行提示词工程命令：
   ```bash
   python scripts/prompt_engineer.py --product "vacuum cup" --material "magnesium alloy" --function "pop-up lid" --style "minimalist" --output "data/prompts.json"
   ```
3. 羊专家执行ControlNet精准控制命令：
   ```bash
   python scripts/controlnet_controller.py --image "data/reference_images/cup_001.jpg" --control_type "canny" --weight 1.0 --output "data/controlnet_config.json"
   ```
4. 羊专家执行LoRA模型应用命令：
   ```bash
   python scripts/lora_applier.py --product "vacuum cup" --lora_type "material" --lora_weight 1.0 --output "data/lora_config.json"
   ```
5. 羊专家执行生成产品效果图命令：
   ```bash
   python scripts/image_generator.py --prompt "data/prompts.json" --controlnet "data/controlnet_config.json" --lora "data/lora_config.json" --output "data/generated_images/"
   ```
6. 羊专家输出生成图文件：`data/generated_images/cup_001.png`
7. 猴专家接收生成图文件，进行参数调优

**输出文件**：
- `data/prompts.json`
- `data/controlnet_config.json`
- `data/lora_config.json`
- `data/generated_images/cup_001.png`
- `data/generated_images/metadata.json`

### 案例2：根据优化后的参数重新生成
**需求**：根据猴专家优化后的参数，重新生成效果图

**执行步骤**：
1. 猴专家输出优化后的参数配置：`data/optimized_params.json`
2. 羊专家读取优化后的参数配置：
   ```bash
   python scripts/image_generator.py --prompt "data/prompts.json" --controlnet "data/controlnet_config.json" --lora "data/lora_config.json" --params "data/optimized_params.json" --output "data/generated_images_v2/"
   ```
3. 羊专家输出重新生成的图片：`data/generated_images_v2/cup_001.png`
4. 鸡专家接收重新生成的图片，进行质量评审

**输出文件**：
- `data/generated_images_v2/cup_001.png`
- `data/generated_images_v2/metadata.json`

## 常见问题解答 (FAQ)#

### Q1：生成图质量不佳怎么办？
**A**：优化提示词（增加权重、调整负面提示词）、调整ControlNet权重、更换LoRA模型。

### Q2：生成图与参考图差异较大怎么办？
**A**：提高ControlNet权重（如：从1.0提高到1.2）、调整提示词权重（如：增加参考图特征词的权重）。

### Q3：生成图风格不一致怎么办？
**A**：使用风格LoRA模型（如：简约风格LoRA）、在提示词中明确指定风格（如：`minimalist style`）。

## 技能包质量#

本技能包设计目标质量分数：**95+**（目标：100/100）

### 质量改进记录
| 版本 | 日期 | 改进内容 |
|------|------|---------|
| v1.0.0 | 2026-06-06 | 初始版本 |

---

**技能包结束**

*本技能包是十二生肖团的生图核心，负责生成高质量产品效果图。*
